//Wikipedia defines drag & drop as the action of (or support for the action of)
// clicking on a virtual object and dragging it to a different location or onto
// another virtual object. In general, it can be used to invoke many kinds of actions,  
//or create various types of associations between two abstract objects. 

#include <wx/wx.h>

class MyApp : public wxApp
{
public:
    virtual bool OnInit();
    
};

